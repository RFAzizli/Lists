package Lists;
public interface ListIterator {
    /*
        hasNext() -checks if there is next element in the list
     */
    boolean hasNext();
    /*
        next() - returns the next Object from the list
     */
    Object next();
}

